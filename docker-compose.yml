services:
  bex-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bex-web
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    volumes:
      - ./data-protection:/mnt/data-protection
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8080/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - bex-network

networks:
  bex-network:
    driver: bridge
